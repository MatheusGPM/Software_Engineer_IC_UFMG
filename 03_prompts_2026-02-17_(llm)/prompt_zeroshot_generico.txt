Tarefa: Você é uma assistente de engenharia de software especializada em testes unitários para projetos Java com Maven. (Define o papel e força a saída esperada como código de teste, evitando respostas genéricas.)

Contexto do projeto:
• Repositório: [NOME_DO_REPOSITORIO] (Identifica o projeto e ajuda na rastreabilidade/reprodutibilidade do experimento.)
• Módulo Maven: [MODULO_MAVEN] (ex.: webmagic-core) (Delimita o classpath correto e garante que o teste seja gerado/rodado no módulo certo.)
• Framework de testes: JUnit (Garante compatibilidade com o framework já usado no projeto, evitando alternativas não suportadas.)

Método-alvo:
• Assinatura: [ASSINATURA_DO_METODO] (Remove ambiguidade sobre parâmetros/retorno/exceções e permite chamadas e asserts corretos.)
• Arquivo/classe: [CAMINHO_DA_CLASSE] (Permite inferir pacote/imports e localizar dependências diretas relevantes para o teste.)

Código relevante:
[TRECHO_DO_CODIGO_DO_METODO_E_DEPENDENCIAS_DIRETAS]
(Fornece o comportamento real do método; sem isso o modelo tende a supor casos e oráculos frágeis.)

Restrições obrigatórias:
(1) Gere um teste unitário em Java que exercite o método-alvo de forma significativa.
(2) O teste deve ser determinístico
(3) Não adicione novas dependências no pom.xml. Use apenas o que já existe no projeto. (Preserva o ambiente e garante comparabilidade entre baseline e teste gerado.)
(4) Evite sleep, random, I/O externo e qualquer flakiness. (Reduz instabilidade e mantém execução rápida e confiável no CI.)
(5) O teste deve compilar e executar via:
mvn -pl [MODULO_MAVEN] -DskipITs=true test
(Ancora o teste no comando real do pipeline, evitando soluções que não rodam no setup.)
(6) Informe o caminho exato do arquivo a ser criado em src/test/java/ e forneça o conteúdo completo do arquivo. (Facilita integração automática e evita ambiguidades de pacote/nome/imports.)

Saída esperada:
• Caminho do arquivo de teste (src/test/java/...) (Padroniza a inserção do artefato e a coleta no repositório.)
• Conteúdo completo do teste
